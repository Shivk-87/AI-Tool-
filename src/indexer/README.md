# Repo Fetcher

This module provides a basic repo fetcher that clones a repository (read-only) into a staging directory.

Usage:
- Provide a public repo URL for the sample test, or supply a read-only PAT via environment variable for private repos.

Note: This is a lightweight starter implementation for the MVP indexer.
