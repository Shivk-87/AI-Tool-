*** Begin Patch
*** Add File: charts/codebase-steward/templates/secretproviderclass.yaml
+apiVersion: secrets-store.csi.x-k8s.io/v1
+kind: SecretProviderClass
+metadata:
+  name: {{ include "codebase-steward.fullname" . | default "codebase-steward" }}-vault-spc
+spec:
+  provider: vault
+  parameters:
+    # Vault server address
+    vaultAddress: "{{ .Values.vault.address }}"
+    # Kubernetes auth role name configured in Vault
+    roleName: "{{ .Values.vault.role }}"
+    # Path in Vault KV (example: secret/data/codebase-steward)
+    objects: |
+      - objectName: "{{ .Values.vault.secretPath }}"
+        objectType: "kv"
+        objectVersion: ""
+    # kv v2 by default; adjust vaultKVVersion if needed (1 or 2)
+    vaultKVVersion: "{{ .Values.vault.kvVersion | default "2" }}"
+
+  # Optional: map vault objects into k8s Secret objects (uncomment if needed)
+  # secretObjects:
+  #  - secretName: codebase-steward-creds
+  #    type: Opaque
+  #    data:
+  #      - objectName: "{{ .Values.vault.secretPath }}"
+  #        key: token
+
*** End Patch
