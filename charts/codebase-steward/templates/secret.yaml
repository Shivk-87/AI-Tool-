apiVersion: v1
kind: Secret
metadata:
  name: codebase-steward-secrets
type: Opaque
stringData:
  # Placeholder for model storage credentials or API keys; recommended to inject via Vault
  MODEL_CREDENTIALS: ""
