apiVersion: v2
name: codebase-steward
description: Helm chart for AI-Assisted Enterprise Codebase Steward (on-prem)
version: 0.1.0
appVersion: "0.1.0"
# Add dependencies like Milvus via requirements in Helm 3 by adding to Chart.yaml 'dependencies' if needed
